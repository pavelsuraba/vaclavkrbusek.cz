import React from 'react';

import Layout from 'layout/Layout';
import CommingSoon from 'layout/Home/CommingSoon';

export default (props) => (
    <Layout {...props}>
        <CommingSoon />
    </Layout>
);
